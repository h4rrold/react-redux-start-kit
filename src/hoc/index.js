import { requireAuth } from "hoc/routes/requireAuth";
import withLayout from "hoc/layout/withLayout";
import withCenterLayout from "hoc/layout/withCenterLayout";
import withMainLayout from "hoc/layout/withMainLayout";

export { requireAuth, withLayout, withCenterLayout, withMainLayout };
